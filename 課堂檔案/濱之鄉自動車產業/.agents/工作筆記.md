# 濱之鄉自動車產業 工作筆記

最後更新：2026-07-20

## 目前進度

影片分析鏈路已完成：影片 → 字幕辨識 → 課例分析 → 12 張抹茶綠繪本插圖 → 16:9 簡報 → 直式插圖 → 新媒體發布包。

新媒體包位置：`output/新媒體文案包_濱之鄉自動車產業_20260720/`

- `FB.txt` / `IG.txt` / `Reels_腳本.txt` / `字幕短句.txt`
- `hamanosato_auto_industry_reels_30s.mp4`　乾淨版，FB 用，另上傳 SRT
- `hamanosato_auto_industry_reels_30s_硬字幕.mp4`　IG 用（IG 不支援上傳 SRT）
- `hamanosato_auto_industry_reels.zh_TW.srt`
- 發布動作由使用者手動執行，尚未發布

版控：commit `80d494e`，`main` ahead origin/main 1，**尚未 push**。
mp4 依 `.gitignore` 規則不進 git，只存在 Google Drive。
`_archive/` 與 drawings 的 `_approved`/`_clean`/`_v2` 中間版本已加入 .gitignore。

## 待辦：把簡報掛到課堂觀察頁

目標：將 `output/簡報/slides_template_26pt_semantic.html` 加入
<https://teaching-3b748.web.app/classroom-observation.html> 並新增縮圖卡片。

**卡住的原因**：該頁的來源檔不在這台電腦上。

- 本機 `G:/我的雲端硬碟/codex/平平校長的AI學習作品/deploy-public/` 是 2026-06-26 舊版
- 缺 `classroom-observation.html` 與 6 張縮圖中的 5 張
- 線上 10 個檔案逐一比對**全部不同**（index 線上多 4KB、style.css 多 2.3KB、ai-learning 少 6KB）
- 從這台 `firebase deploy` 會整站回滾並刪掉該頁 → 禁止直接部署

**已查明的既有格式**（可直接照抄）

- 卡片：`<div class="ai-card purple video-card reveal">` + `<img class="video-card-thumb" src="assets/xxx-thumb.png">` + `<div class="video-card-body">` 內含 h3、p、`<a class="ai-card-link" target="_blank" rel="noopener">開啟簡報 ↗</a>`
- 縮圖規格：1280×720 PNG，放在 `assets/`
- 簡報本體慣例放 GitHub Pages：`hsuyiping-rgb.github.io/<repo>/`
- 現有 9 張卡片

**下次開工要先決定兩件事**

1. 來源檔以哪邊為準：抓線上回本機／使用者自己貼／來源在別台電腦
2. 簡報發布位置：GitHub Pages 新 repo（比照現行）或放進 teaching-3b748 站內

注意：`slides_template_26pt_semantic.html` 只有 7.5KB，但外連 `../drawings/*_continuity.png` 共 33MB，發布時必須連圖一起，否則版面全空。
