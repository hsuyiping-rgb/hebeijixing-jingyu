desktop.ini
Thumbs.db
.DS_Store
*.tmp
~$*

.env
.env.*
*.key
credentials.*
secrets.*

.claude/
.codex/
.firebase/
.playwright-cli/
__pycache__/
*.pyc

node_modules/
dist/
build/
.cache/

# Large generated media stays in Google Drive, not Git.
output/影音/
output/課堂檔案/**/影音/*.mp4
課堂檔案/**/影音/*.mp4
課堂檔案/**/聲音與影片/**/*.mp4
課堂檔案/**/output/**/*.mp4
課堂檔案/**/output/**/*.mp3

# 學習共同體課堂影片分析 底下的大型媒體同樣只留 Google Drive（避免超過 GitHub 100MB 上限）。
學習共同體課堂影片分析/**/*.mp4
學習共同體課堂影片分析/**/*.mp3
學習共同體課堂影片分析/**/*.m4a
學習共同體課堂影片分析/**/*.wav
學習共同體課堂影片分析/**/*.mov
學習共同體課堂影片分析/**/*.webm

# 被淘汰的簡報草稿留在 Google Drive；其中有超過 GitHub 100MB 單檔上限的 pptx。
課堂檔案/**/_archive/

# 生圖過程的中間版本留在本機，只版控採用的成品。
課堂檔案/**/output/drawings/*_approved.png
課堂檔案/**/output/drawings/*_clean.png
課堂檔案/**/output/drawings/*_v2.png

# Generated workflow markers.
*.ok

# Deployment worktrees are kept locally, not as nested Git links.
課堂檔案/**/deploy_github_pages/

# Firebase 部署暫存資料夾（含大型投影片 PNG），只留本機／Google Drive，不進 Git。
# 內容是 slides.html + images/ 的複本，重建成本低，不需版控。
課堂檔案/**/output/firebase_*/
學習共同體課堂影片分析/**/output/firebase_*/

# AI 插圖的未壓縮原始 PNG（約 60MB／課例）。版控的是 images/ 底下的
# JPEG q90 網頁版（同解析度、體積約 1/6）。原圖不可重現，務必留在
# 本機與 Google Drive，只是不進 Git。
學習共同體課堂影片分析/**/output/images_original/
課堂檔案/**/slides_deploy/

# 以下兩個路徑是各自獨立的 git repo（有自己的 GitHub 遠端與備份），
# 不是本 repo 的素材。若 git add 會產生壞掉的 gitlink：外層只記一個
# commit SHA、不含檔案內容，clone 後拿到空資料夾且無從取得。
# 要更新它們請 cd 進去各自 commit / push。
#   宇津睦台小學 → https://github.com/hsuyiping-rgb/slc-mochimochi-lesson-study
#   鴨嘴獸探究   → https://github.com/hsuyiping-rgb/2026-japan-slc-presentation
學習共同體課堂影片分析/宇津睦台小學/
課堂檔案/2026 日本 SLC 國中生物：未知動物 A／鴨嘴獸探究/

# Firebase Hosting 部署產物（由來源檔複製而來，不納入版控）。
public/
