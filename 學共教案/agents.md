# 學共教案（專案藍圖）

> 本檔為跨 Agent 通用的專案藍圖（AGENTS.md 開放標準）。任何 Agent 的每個 session 都應先讀本檔＋`handoff.md`。

## 專案簡介

開發並維護 **`slc-lesson-plan`**——學習共同體公開課四十分鐘教案撰寫技能。
技能依新北市自強國小「教學方案設計」制式表產出完整教案，支援共有課題／挑戰性課題兩種主軸，
涵蓋國語、數學、社會、自然四科，並把「傾聽—串連—回歸」與六大理答策略
（轉引、轉問、反問、提示、釐清、深究）嵌進教師支援欄的實際台詞與理答預想表。

本資料夾同時是該技能的**預設工作區**：`範本/` 存參照教案，`教材/` 存每次任務的課本教材，
`教案成果/` 存產出的課題設計卡與教案。

## 關鍵時程

<!-- 目前無固定時程；有公開課日期再登錄 -->

## 目標與路線圖

- [x] 階段一：訪談確認規格（骨架、課題型態、理答嵌入層次、教材取得、流程、檢核）
- [x] 階段二：建置技能本體（SKILL.md ＋ 4 份 references ＋ 2 份 templates）
- [x] 階段三：國語、數學科指引（有制式表實例支撐）
- [x] 階段四：自然科指引（滑輪組教案）、社會科指引（濱之鄉脇坂課例）
- [ ] 階段五：**實測一課走完 Step 0 → Step 1 → Step 2**，驗證流程與硬檢核
- [ ] 階段六：Step 4 的 docx 轉檔腳本（以 `範本/公開課教案參考格式.docx` 為底，python-docx 填表）
- [ ] 待補：國內社會科公開課教案實例（制式表格式）

## 資料夾結構

```
學共教案/
├── agents.md                     本檔
├── handoff.md                    交接檔
├── 範本/                          參照教案（唯讀，不要覆寫）★不進版控，見下
│   ├── 公開課教案參考格式.docx                    ← 唯一骨架來源
│   ├── 新北市中和區自強國小106學年度 國語科公開課教案1126.docx
│   ├── 新北市中和區自強國小110學年度 數學科 公開課教案(許以平)0505-1.docx
│   ├── 自然科_巧妙的施力工具_滑輪_六年級.docx
│   └── 宇津木台小學校五年級國語教案1.pdf／2.pdf   ← 同一份指導案的兩頁，掃描無文字層
├── 教材/                          每次任務一個子資料夾：<年級>_<科目>_<單元>/
├── 教案成果/                      每次任務一個子資料夾：<年級>_<科目>_<單元>_<日期>/
└── skills/slc-lesson-plan/       技能本體（單一真實來源）
    ├── SKILL.md
    ├── references/  理答六策略／制式表欄位對照／日本教案參照／科目_國語·數學·社會·自然
    └── templates/   教案範本.md／課題設計卡.md
```

## 同步層級（本專案初始化至第 3 層級）

| 層級 | 平台 | 位置 | 讀取時機 |
|------|------|------|---------|
| L1 | 本地（GDrive） | `agents.md`＋`handoff.md` | 每個 session |
| L2 | GitHub | 隨母 repo [hsuyiping-rgb/hebeijixing-jingyu](https://github.com/hsuyiping-rgb/hebeijixing-jingyu)（**PUBLIC**）；本專案**不另建巢狀 repo** | 指定時 |
| L3 | Obsidian | `學共教案/專案工作流程.md` | 有需要時 |

## 技能相關約定

- **`範本/` 不進版控**（母 repo 是 PUBLIC，而數學科教案內含 307 班座位表與 24 位學生姓名）。
  母 repo 的 `.gitignore` 已登錄 `學共教案/範本/`。**換電腦 clone 後不會有這些檔案**，
  需自行從雲端硬碟或原始出處補齊，技能才跑得完 Step 4 的 docx 轉檔。
- **技能的單一真實來源**是 `skills/slc-lesson-plan/`。`~/.claude/skills/slc-lesson-plan/` 是複本。
  **改技能一律先改這裡，再 `cp -r` 覆蓋 `~/.claude/skills/`**，不要反向操作。
- 目前**未同步**到 `~/.codex`、`~/.agents`。要在 Codex 也用時再說。
- `範本/` 的檔案是唯讀參照，**不要覆寫**。
- 技能的紅線繼承自 `exam-composer`：**Agent 不得輸入任何密碼、不得繞過下載保護機制。**

## 工作約定

- 任何 Agent、任何電腦：**開工先讀 `handoff.md`，收工必更新 `handoff.md`**
- 修改共用檔案前先讀最新內容，避免覆蓋其他 Agent 的變更
- 所有回應與文件使用繁體中文
- 修改前先確認計畫，優先保留原有資料結構
- 母 repo 在 GDrive 上，git 設定已含 `windows.appendAtomically false`
