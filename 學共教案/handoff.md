# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。本檔只放交接必需的精簡資訊，詳細脈絡放 Obsidian。

## ⏯️ 目前做到哪

技能已實測**四課**（數學、國語、自然、社會各一）。

**2026-09-12 這一輪做了三件事：**

1. **版面規格定案**：使用者手改了自然科含圖版的排版，逆向比對出四條規則並寫進 `build_docx.py`
   （全文新細明體 12pt、理據表欄寬 1.4／3.6／13.4 cm 並寫進 tblGrid、各節次重點列最小高度 4.6 cm、
   附件學習單表格撐滿版面寬），存檔前 `finalize_format()` 一次套用。四課 docx 全部重產。
2. **串連進程模擬**：新增 JSON 鍵 `串連進程模擬` → 「附錄二」表，轉引／轉問／深究各一段、
   每段三位學生三段發言、第五欄寫「這一步在串什麼」；流程表支援欄同步放骨架並標「見附錄二 A／B／C」。
   四課的 JSON、md、docx 都補了。規則寫進 SKILL.md、制式表欄位對照.md（九之二）、README。
3. **自然課學習單夾圖**：4/19 天氣圖放進【一】題與觀測表之間、4/20 天氣圖放進附件二資料紙條；
   說明句改成兩版都通用的寫法。

## 🚦 目前狀態

- 技能可用。硬檢核 12 條（新增「串連進程模擬三段」）。
- **本輪尚未 commit**：技能 4 檔（`build_docx.py`、`SKILL.md`、`README.md`、`references/制式表欄位對照.md`）
  ＋四課的 `教案內容.json`／`教案.md`／`教案.docx`。`~/.claude/skills/` 複本已 cp 並 `chezmoi re-add`，
  dotfiles 也還沒 push。
- `學共教案/` 根目錄有未追蹤的 **`課堂理答：釐清與深究.docx`**（使用者放的，非本輪產出），是否進版控待使用者決定。
- **四課教案都缺 Step 0 欄位**（班級人數、日期節次、教學者、備課成員、特殊學生），文中以 `〔待補〕` 標記。
  串連進程模擬裡的生1／生2／生3 也等特殊學生資料補上後改成指名。

## ➡️ 下一步

1. **commit + push**（母 repo）與 dotfiles push——使用者說收工時未明說要提交，先留著。
2. 等使用者提供 Step 0 資料，補四課基本欄位、觀課焦點第 3 條與串連進程的指名對象，重跑 `build_docx.py`
   （國語、自然、社會各兩份 docx，別漏）。
3. 社會課 `教案.md` 的串連進程章節編為「附錄三」（因原本已有附錄二：理答預想表），docx 標題仍是「附錄二」；
   要不要統一，問使用者。
4. （延後）補國內社會科公開課教案實例。

## ⚠️ 注意事項

- **版面字型是新細明體**（依使用者定稿），不是公文常見的標楷體；要換改 `build_docx.py` 的 `BODY_FONT`。
- **`.git/packed-refs.lock` 殘留鎖檔仍在母 repo**（`和北極星境遇/.git/`）。commit 會噴警告但成功。
  確認沒 git 程序時 `rm` 掉即可。**它在母 repo，不在 `學共教案/`。**
- **母 repo `hebeijixing-jingyu` 是 PUBLIC。** 不進版控的五類：`範本/`、`教材/`、課文附件、
  課本圖附件（`附件_課本*`、`*（含課本圖）.docx`）、`學共教案/tmp/`。推之前用 `git add -An` 空跑一次。
- **產含圖版與無圖版要各跑一次**：含圖版不帶旗標、輸出檔名帶「（含課本圖）」；無圖版加 `--無圖`、輸出 `教案.docx`。
- **auto mode 的權限分類器會擋 commit/push**，若使用者當輪沒明說要提交。做完先回報、等使用者說「commit」再動。
- **`範本/公開課教案參考格式.docx` 只在這台電腦（DESKTOP-31QBU95）。** 換電腦要手動帶 `範本/`。
- **輸出的 docx 被 Word 開著會 `PermissionError`**，看到 `~$…docx` 鎖檔就先請使用者關掉；
  本輪的作法是先輸出到暫名，關掉後再覆蓋。
- **LibreOffice 渲染檢查**：`"/c/Program Files/LibreOffice/program/soffice.exe" --headless --convert-to pdf`，
  再用 PyMuPDF 出縮圖拼 contact sheet 看整體版面。
- **技能的單一真實來源是 `skills/slc-lesson-plan/`**，`~/.claude/skills/` 是複本，由 chezmoi 管。
  改完 `cp` 過去，既有檔 `chezmoi re-add`、**新檔要 `chezmoi add`**。
- 制式表三段流程是**導入與發現／探究與深化／挑戰與聚斂**。「伸展跳躍」是難度用詞。
- **`領域課程綱要` 是二維表**，左上角斜線表頭是繪圖物件，**不要覆寫**。
- 讀寫 docx 一律加 **`python -X utf8`**；長 python 腳本用 Write 寫檔再跑，不用 heredoc。
- **改檔與 `git add` 不要寫在同一個指令裡**，提交前先看 `git diff --cached`。

## 🕐 最後更新

- 時間：2026-09-12
- 更新者：**Claude Opus 5** @ DESKTOP-31QBU95
- Git push：母 repo ⏳ 待推（本輪未 commit，等使用者點頭）；dotfiles ⏳ 待推
